import { RiskBridge } from "./components/risk-bridge";

export const metadata = {
  title: "Risk Bridge | AlgoVigilance Vigilance",
  description:
    "Follow a safety signal through the full decision chain — from detection to regulatory action",
};

export default function RiskBridgePage() {
  return <RiskBridge />;
}
