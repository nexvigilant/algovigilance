import { CaseTracker } from "./components/case-tracker";

export const metadata = {
  title: "Case Tracker | AlgoVigilance Vigilance",
  description:
    "Track safety cases through every stage — from intake to closure",
};

export default function CaseTrackerPage() {
  return <CaseTracker />;
}
