import { WarGameDashboard } from './components/war-game-dashboard';

export const metadata = {
  title: 'Drug Design War-Game | AlgoVigilance Vigilance',
  description: 'Reverse PV drug design — exploit FAERS safety gaps with Nash equilibrium competitive positioning',
};

export default function DrugDesignPage() {
  return <WarGameDashboard />;
}
