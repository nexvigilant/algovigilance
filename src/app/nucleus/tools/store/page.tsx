import { AppStore } from './components/app-store';

export const metadata = {
  title: 'Ecosystem App Store | AlgoVigilance Tools',
  description: 'Browse ecosystem applications directly in Nucleus',
};

export default function StorePage() {
  return <AppStore />;
}
