import { EcosystemMesh } from './components/ecosystem-mesh';

export const metadata = {
  title: 'Ecosystem Mesh | AlgoVigilance Tools',
  description: 'Full node-to-node navigation graph across all domains and app surfaces',
};

export default function MeshPage() {
  return <EcosystemMesh />;
}
