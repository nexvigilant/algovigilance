// ============================================================================
// PV EXTRACTION PATTERNS — SIGNAL DETECTION (shared types)
// ============================================================================

/** Signal strength classification */
export type SignalStrength = 'none' | 'weak' | 'moderate' | 'strong';
